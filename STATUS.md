# Release status

Current status: **Candidate** — E2E fine-tuning tutorial source complete, awaiting supported clean-runtime execution. The pipeline package (inference contract, digest-pinned Tatoeba corpus, chrF/BLEU metrics, bounded decoder fine-tuning, safetensors adapter), 35 offline unit tests (including the notebook-parity tests) plus 2 model-backed tests, one executed local CPU run of the committed notebook blob, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/marianmt_translation_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.

The earlier `TASK-INFERENCE` notebook had a recorded Kaggle CPU pass (blob `c9155c925dfc` at `5df0317`, 2026-09-14); that evidence is retained in `docs/release-verification.md` as history and does not carry over to the `E2E` notebook, which is a different blob with a different default path.

No licence gate is open for DIMER hosting (Apache-2.0 weights). The one standing review item is unchanged: the only upstream weight file is a PyTorch pickle, digest-verified before it is opened and loaded with `torch.load(weights_only=True)`; see `MODEL_CARD.md` and `docs/WEIGHTS.md`. The adaptation sample is fetched at run time from OPUS (Tatoeba en–tl, CC BY 2.0 FR, one pinned 312 KB zip) and is not redistributed by this repository.
