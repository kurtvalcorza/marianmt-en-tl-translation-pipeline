# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/marianmt-en-tl-translation-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/marianmt-en-tl-translation-pipeline/blob/main/tutorials/marianmt_translation_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-Helsinki--NLP%2Fopus--mt--en--tl-ffcc4d?style=flat)](https://huggingface.co/Helsinki-NLP/opus-mt-en-tl)
[![Upstream](https://img.shields.io/badge/Upstream-Helsinki--NLP%2FOPUS--MT--train-181717?style=flat&logo=github&logoColor=white)](https://github.com/Helsinki-NLP/OPUS-MT-train)
[![arXiv](https://img.shields.io/badge/arXiv-1804.00344-b31b1b.svg)](https://arxiv.org/abs/1804.00344)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's three
pipeline modules (`pipeline.py`, `samples.py`, `metrics.py`), the model identity, the snapshot manifest and the
runtime pins, so the exported `.ipynb` keeps working without this repository being reachable. Do not edit the
notebook by hand; edit the package or the template and regenerate (`python tools/build_notebook.py`; `--check` is
enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `marianmt_translation_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated) | batched English→Tagalog translation (EN→TL only) with `Helsinki-NLP/opus-mt-en-tl` — the pickle checkpoint `pytorch_model.bin` digest-verified against the inline manifest before it is opened and loaded with `weights_only=True`; the inference contract with its input manifest, rejection probe and `stopped_by` semantics; a digest-pinned real parallel corpus (Tatoeba en–tl via OPUS) validated and split without source leakage; corpus chrF and BLEU against references beside the copy-source baseline; bounded fine-tuning of the last two decoder blocks with validation-chrF epoch selection; held-out evaluation with a three-way comparison; translation of unseen sentences; safetensors adapter export with verified reload parity | CPU (CUDA used automatically when present); float32 | 1,200 / 200 / 300 Tatoeba pairs from one pinned 312 KB zip fetched at run time (CC BY 2.0 FR) | CSV / JSON / JSONL of `{id, source, target}` records (gated off by default) | ~2 min of model time on CPU after the 296 MB checkpoint download | **Release-grade** — clean-runtime Kaggle Tesla T4 execution of `292d4fa` / `1adc963a` recorded 2026-09-19 in `../docs/release-verification.md` |

## What changed from the inference-only tutorial

The previous notebook (`TASK-INFERENCE`) translated three synthetic sentences and reported a `not-measurable` verdict because the repository shipped no metric and the sample had no references. This notebook keeps that inference contract as Section 5 and adds the adaptation contract around it: a real referenced corpus, two reference-based metrics implemented in the carried `metrics.py`, a copy-source baseline, a bounded fine-tuning, and an artifact that reloads. `evaluation_report` now scores references when they are supplied (`measured` / `measured-small-sample`) and still returns `not-measurable` without them.

## Conformance notes

- **Standalone carrier (ST1–ST8):** the primary path performs no clone, repository install, or repository import. Section 2 embeds the repository's 3 modules verbatim in topological dependency order (each tagged `metadata.dimer.embedded_module`; the only rewrites make `DEFAULT_WEIGHTS_DIR` working-directory-relative and remove internal relative package imports), Section 3 carries the 8-file snapshot manifest inline and asserts it against the module's constants before fetching, and `NOTEBOOK_SOURCE` records the repository, revision and module digests in the exports.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried modules, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Pickle trust boundary (MOD12):** the only upstream weight file is `pytorch_model.bin`; the opening cell says so in bold, Section 3's generated text states that `verify_snapshot` re-hashes every file and raises on a mismatch before `from_pretrained` runs, and the carried module's loader (`use_safetensors=False`, `weights_only=True`) is the code that enforces it. The export records `weight_format` and the pickle's manifest SHA-256, and the adapter manifest records the same digest as its base.
- **Model acquisition (MOD1–MOD9):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries a fresh clone lacks, at the pinned revision; `verify_snapshot` re-hashes every entry; `MarianMTTranslationPipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files (`local_files_only=True`, `trust_remote_code=False`); no cell outside the carried modules calls `huggingface_hub`, `transformers`, `safetensors` or `urllib`.
- **Data (DAT1–DAT9, ST6):** the default sample is one pinned object — `en-tl.txt.zip` (312,459 bytes, SHA-256 `9abddc7d…`) from OPUS at `object.pouta.csc.fi` — refused on any mismatch before it is read, read member by member with no `extractall`, filtered and de-duplicated case-insensitively on the English side, and cut by a seeded shuffle into 1,200 / 200 / 300 disjoint records (`check_split_disjoint` asserts it). The template adds a second *External access* bullet naming the host, the byte count and the CC BY 2.0 FR licence.
- **Stages (§4.1):** `fetch_corpus` + `build_sample_dataset` / `load_byod_dataset` + `split_dataset` (sample acquisition) → `validate_dataset` + `check_split_disjoint` + refusal probes (validation, with three digests) → `validate_inputs` + `pipe.translate` with sanity checks (inference contract) → `copy_source_baseline` + `pipe.evaluate` on the frozen model (baselines) → `pipe.adapt` (bounded fine-tuning) → `pipe.evaluate` on validation and test with the comparison (evaluation) → `pipe.translate` on unseen sentences + `evaluation_report` with references (inference) → `pipe.save_artifact` → `MarianMTTranslationPipeline.from_artifact` with the parity assertion (export and reload).
- **Score semantics (UNC1–UNC4):** the pipeline emits no probability, confidence or score; chrF and BLEU are corpus-level agreement with references (own implementations, sacrebleu-style, not sacrebleu-identical); no acceptance threshold ships, and `stopped_by == 'max_new_tokens'` is explained as a truncated output. Six unseen sentences are scored with a `measured-small-sample` verdict that names the missing dispersion.
- **Baselines and evidence (EVAL6/EVAL10/EVAL11):** the copy-source baseline and the frozen model are scored on the same 300 test sentences as the adapted model, the frozen BLEU is read against the upstream README's Tatoeba figure as a sanity check, and every metric is labelled as one seeded split of one corpus with no dispersion estimate.
- **Split integrity (SPL1–SPL8):** the split is seeded and source-disjoint; validation selects the epoch and the test split is used for nothing but the final evaluation; the notebook states that near-duplicate pairs from one document or session must be split by that grouping.
- **Ceilings (VAL6):** `MAX_BATCH` (16), `MAX_TEXT_CHARS` (4,000), `MAX_INPUT_TOKENS` (512, rejected not truncated), `MAX_NEW_TOKENS` (512), `MAX_NUM_BEAMS` (8), the record bounds (8..20,000), the id pattern and the training-only 128-piece truncation are stated in the Prerequisites and printed before the model runs; four dataset refusals and one settings refusal are demonstrated.
- **BYOD (DAT10–DAT19):** `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; when enabled, user pairs flow through the same validation, split, baselines, fine-tuning, evaluation, inference, export and reload cells. The expected schema and the privacy guidance — a proprietary translation memory is restricted data — are stated in the Prerequisites and in Section 4, and the training split is written to `outputs/marianmt_translation_train.csv` in that shape.
- `sacremoses` is not pinned and not installed: the notebook's opening **Environment note** says the tokenizer prints one `Recommended: pip install sacremoses.` warning and uses the identity normaliser; every recorded number was produced in that state.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
